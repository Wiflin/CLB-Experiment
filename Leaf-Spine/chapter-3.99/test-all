#!/bin/bash

method=("CLB" "CLOVE" "CONGA" "ECMP" "HERMES")
# load=`seq 0.1 0.1 1`

# for i in ${load[@]}
# do



for j in ${method[@]}
do
	rm -rf $j
	mkdir $j
	cp test-bash part1.tcl InputFlow* ./$j

	sdir=`pwd` 
	cdir="./$j"
	cd $cdir
	echo `pwd`
	./test-bash $j $1 & 
	cd $sdir

done

